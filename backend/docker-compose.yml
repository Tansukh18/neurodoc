version: "3.8"

services:
  backend:
    build: .
    container_name: neurodoc-backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
